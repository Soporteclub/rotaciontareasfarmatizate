# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
# FIX (PROC-10): keep the example tracked
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ─── Runtime / Local ──────────────────────────────────────────────
local-*
server.log
*.log
dev.log
dev.out.log

# Database (PostgreSQL/Neon — connection string lives in .env, never committed)

# FIX (SEC-03): backups now live in /data/ (not public/). Never commit them.
public/backup.json
/data/

# ─── Sandbox / z.ai specific (not needed locally) ────────────────
.claude
.z-ai-config
/skills/
Caddyfile
serve.pid
server.pid
agent-ctx/
upload/
download/
worklog.md
test
prompt
examples/
.kilo/
*.pid
